plugins {
    id "java"
    id "idea"
    id "eclipse"
}

version '2.0'

ext.moduleName = 'org.l2j.mmocore'

sourceSets {
    main {
        java {
            srcDirs = ['src/main/org.l2j.mmocore']
        }
        resources {
            srcDirs = ['src/main/resources']
        }
    }
}

jar {
    baseName 'mmocore'
}